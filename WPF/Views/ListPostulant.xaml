<UserControl x:Class="WPF.Views.ListPostulant"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ListView ItemsSource="{Binding Postulants, UpdateSourceTrigger=PropertyChanged}">
        <ListView.View>
            <GridView>
                <GridViewColumn Width="150px" Header="Intitule" DisplayMemberBinding="{Binding Path=EmployeId}" />
                <GridViewColumn Width="150px" Header="Intitule" DisplayMemberBinding="{Binding Path=NomPrenomPostulant}" />
                <GridViewColumn Width="100px" Header="Date" DisplayMemberBinding="{Binding Path=Date, StringFormat='dd/MM/yyyy'}" />
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
